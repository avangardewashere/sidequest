# Phase 8.3 — Today surface (Tracker)

Companion plan: `phase-8-3-today-surface-plan.md`.

## Checklist

- [x] Plan + tracker; cycles 8.3 in progress
- [x] `GET /api/today/habit-surface` + snapshot types + `Quest.createdAt`
- [x] `fetchTodayDashboard` + `isSnapshotShape` + refresh wiring
- [x] Home sections above Today stack
- [x] Tests + gates + progress-summary + cycles closeout

## Status

Done (2026-05-01).
