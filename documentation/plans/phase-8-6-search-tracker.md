# Phase 8.6 — Search (Tracker)

Companion plan: `phase-8-6-search-plan.md`.

## Checklist

- [x] Plan + tracker; cycles 8.6 in progress
- [x] `GET /api/quests/search` + tests
- [x] Types + `searchQuests()` client
- [x] `GlobalSearchDialog` + app bar Search + RTL smoke
- [x] Cmd/Ctrl+K + guard + test
- [x] Quest detail: LinkPicker + search (remove paste-id)
- [x] QuestForm edit: links + LinkPicker + APIs
- [x] `GET /api/quests/[id]/linked-from` + detail rail + tests
- [x] Tag deep-link audit (detail `Link` + list `onTagClick` → `/quests/view?tag=`; no other tag chip surfaces required changes)
- [x] Closeout: cycles 8.6 done, progress-summary, gates

## Status

Complete.
