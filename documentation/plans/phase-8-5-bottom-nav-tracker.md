# Phase 8.5 — Bottom nav + Capture FAB (Tracker)

Companion plan: `phase-8-5-bottom-nav-plan.md`.

## Checklist

- [x] `AuthenticatedAppShell` + app bar + `BottomNav` + bottom padding
- [x] Capture FAB + sheet (title + tags, one-off create + tags PATCH)
- [x] Remove `DashboardNav` / duplicate `TodayFocusTabBar` from main routes
- [x] Today shell: remove local FAB / tab bar / quick-add; listen for capture event
- [x] Docs, cycles, progress-summary; `app-shell` tests; gates

## Status

**Complete** (2026-05-01). Next: **Phase 8.6**.
