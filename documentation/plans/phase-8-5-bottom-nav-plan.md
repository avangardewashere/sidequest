# Phase 8.5 — Bottom nav + Capture FAB (Plan)

Cycle 8 / Phase 8.5. Companion tracker: `phase-8-5-bottom-nav-tracker.md`.

## Goal

Mobile-first shell: bottom tabs, top app bar, global capture (one-off inbox) with title + tags.

## Scope

Implemented in repo: `AuthenticatedAppShell`, `CaptureQuestSheet`, integration on Today, quests, stats, you; FAB hidden on quest detail and onboarding paths.

## Notes

- Fourth tab uses existing **`/you`** (not a separate `/profile` stub).
- Search / Cmd-K deferred to **Phase 8.6**.

## Out of scope

Phase 8.6 search API, global keyboard shortcut, `LinkPicker` wiring.
