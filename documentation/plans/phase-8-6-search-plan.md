# Phase 8.6 — Search + cross-linking (Plan)

Cycle 8 / Phase 8.6. Companion tracker: `phase-8-6-search-tracker.md`.

## Goal

User-scoped quest search, global search UI with Cmd/Ctrl+K, LinkPicker wired to search on detail + edit form, linked-from backrefs, and consistent tag deep links.

## Out of scope

Atlas Search / dedicated text indexes (defer unless performance requires).
