# Phase 8.1 — Quest list redesign (Tracker)

Companion plan: `phase-8-1-quest-list-redesign-plan.md`.

## Checklist

- [x] Plan + tracker; cycles status
- [x] `Quest.tags` typing + `quest-selectors` list view helpers + unit tests
- [x] `quests/view`: tabs, sticky bar, Card/Badge/Button rows, child counts (`quest-list-view-client.tsx` + thin `page.tsx`)
- [x] Tag chips + `?tag=` URL sync; habit row chrome (streak `0` default)
- [x] RTL or integration-style tests
- [x] Gates + progress-summary + cycles closeout

## Status

Done (2026-05-01).
